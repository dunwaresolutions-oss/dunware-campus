-r base.txt

# Campus — development & CI extras.
pytest>=8.3
pytest-django>=4.9
pytest-cov>=5.0
ruff>=0.6                     # lint + format
bandit>=1.7                   # security static analysis (CI gate)
pip-audit>=2.7               # dependency CVE audit (CI gate)
django-extensions>=3.2
model-bakery>=1.19           # synthetic test fixtures only
